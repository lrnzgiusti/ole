use egui::Context;

pub fn draw_glow(_ctx: &Context) {
    // Glow effect placeholder - will be implemented in Phase 8
    // Uses layer painter to draw concentric transparent shapes behind bright elements
}
